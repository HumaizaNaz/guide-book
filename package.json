{
  "name": "dev-guides",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cp *.md website/ && cd website && npm run build",
    "dev": "cd website && npm run dev"
  },
  "dependencies": {
    "next": "16.2.6"
  }
}
